{% apply spaceless %}
<nav data-action-menu class="actions-menu uk-navbar">
    {% if item %}
        <a class="uk-button uk-button-primary uk-button-danger"
           href="{{ path('adminApiApplicationsDelete', { id: item.id}) }}">
            <span class="icon-container"><i class="uk-icon-rz-trash"></i></span>
            <span class="label"><span class="label-text">{% trans %}api.applications.delete{% endtrans %}</span></span>
        </a>
    {% endif %}
</nav>
{% endapply %}
